import styled from "styled-components";
import { dazzledBlue, cadetGrey } from "../../theme/variables";

export const Input = styled.input`
  height: 11vh;
  font-weight: bold;
  color: ${dazzledBlue};
  font-size: 1.3em;
`;

export const TicketPrice = styled.div`
  font-size: 0.5em;
  color: ${cadetGrey};
`;
